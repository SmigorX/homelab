apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: samba
  namespace: argocd
spec:
  project: workloads

  source:
    repoURL: git@github.com:SmigorX/homelab.git
    targetRevision: master
    path: samba/manifests
    directory:
      recurse: true

  destination:
    server: https://kubernetes.default.svc
    namespace: samba

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
